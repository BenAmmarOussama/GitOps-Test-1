apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: pushgateway
  namespace: {{ .Values.namespace.name }}
spec:
  selector:
    matchLabels:
      app: pushgateway
      monitored-by-prometheus: "true"
  namespaceSelector:
    any: true
  endpoints:
    - interval: 15s
      targetPort: web
      path: /metrics